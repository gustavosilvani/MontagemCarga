namespace MontagemCarga.Domain.Enums;

/// <summary>
/// Nível de quebra permitido no agrupamento (item, caixa, pallet).
/// </summary>
public enum NivelQuebraProdutoRoteirizar
{
    Item = 0,
    Caixa = 1,
    Pallet = 2
}
